---
// i18n (SPEC 80): variante EN de /soluciones/[solucion]. Reusa los static paths ES y renderiza la misma página; el locale sale de la URL (/en).
import Page, { getStaticPaths as esPaths } from "../../soluciones/[solucion].astro";
export const getStaticPaths = esPaths;
---

<Page />
