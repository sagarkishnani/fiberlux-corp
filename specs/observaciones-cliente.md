# Observaciones del cliente — checklist de seguimiento

> Fuente: carpeta `~/Downloads/observaciones_fiberlux/` (capturas + videos + textos de WhatsApp de Miguel).
> Registrado: 2026-07-21. Este archivo es la **fuente de verdad** del estado de cada observación.

**Estados:** `Pendiente` · `En curso` · `Hecho` (implementado en rama) · `Mergeado` · `Diferido` · `Necesita aclaración`

---

## Checklist

| # | Observación | Página / Sección | Tipo | Estado | Notas |
|---|---|---|---|---|---|
| 1 | Raya/línea **blanca** arriba (bajo la topbar) al hacer scroll | Home + subservicio · **mobile** | Bug visual | **Hecho** | Causa: html/body con fondo transparente → canvas blanco del navegador en overscroll (Android). Fix: `html { background:#0a0a0a }`. |
| 2 | El **drag se queda a la mitad** y no corre a la siguiente card | Soluciones slider · **mobile** | Bug (motor slider) | **Hecho (pend. verif. Android)** | `snap-mandatory` en soluciones. Verificado en desktop; falta prueba touch real. |
| 3 | La sección **se lagea** al cambiar de testimonio | Testimonios · **mobile** | Bug (motor slider) | **Hecho (pend. verif. Android)** | Sin blur/re-render; era feel del snap → `snap-mandatory`. Si persiste, requiere profiling en el device. |
| 4 | Agregar **degradado de profundidad** + **cambio de color en las letras** (números) | Stats "¿Por qué Fiberlux?" | Mejora diseño | **Hecho (best-effort)** | Panel con degradé de profundidad + cards glass + números con gradiente. |
| 5 | Agregar detalle **glass** al fondo de las cards ISO (no se nota el contenedor en bajo brillo) | Certificaciones (ISO) · mobile | Mejora diseño | **Hecho (best-effort)** | Cards ISO con glass (brillo arriba + magenta abajo + borde). |
| 6 | **Rubros: actualizar íconos** según referencia | Rubros (Nosotros) | Diseño/contenido | **Hecho** | Todos los íconos de rubros remapeados al estilo outline (Lucide), coincidiendo con la referencia. |
| 7 | Bug del **carrusel al dar clic en la flecha** (video 0:07) | Rubros (Nosotros) | Bug (motor slider) | **Hecho (pend. verif. Android)** | Motor: se suprime el touch-settle tras un scroll programático (`suppressSettleUntil`) → la flecha ya no salta. Aplica a los 3. |
| 8 | Se **bugea el carrusel al dar clic en las flechas** (video 0:04) | Catálogo de soluciones | Bug (motor slider) | **Hecho (pend. verif. Android)** | Mismo fix del motor (7). Catálogo ya era `snap-mandatory`. |
| 9 | Subir el **degradado a negro más arriba** (legibilidad) + **raya blanca** | Subservicio hero (modo imagen) · mobile | Mejora + bug | **Hecho** | Raya blanca (=obs_1) + overlay reforzado arriba (degradado vertical extra en mobile). |
| 10 | Más **glass/degradado** en las cards; en bajo brillo no se diferencia la card | Soluciones slider (cards) | Mejora diseño | **Hecho (best-effort)** | Card activa glass (blur+brillo+magenta multi-tono) + glow derecho + grano. |
| 11 | Usar el **mismo texto que casos de éxito** (Gloria) + opción de **cambiar foto por logo** | Testimonios | Contenido + CMS | **Hecho** | Contenido replicado de casos + card usa logo si no hay foto (sin cambio de schema). |
| 12 | Opción de poner el **logo del grupo en vez de las comillas** | Casos de éxito (card) | CMS | **Hecho** | Card de casos: logo en vez de comilla cuando hay logo. + mediaUrl en poster/logo. |
| 13 | Revisar **contenido de testimonios** (texto/logo Boticas + 3ro de casos de éxito) | Testimonios | Contenido | **Hecho** | Testimonios = Boticas + Cámara Arequipa + Gloria (contenido de casos). Avatares vacíos (cliente sube foto/logo). |
| 14a | Un **vector distinto** en el "desafío" de cada categoría | Sección "El desafío" (por categoría) | Asset/contenido | Pendiente | Requiere un gráfico por categoría. |
| 14b | La info **solo se amplía en Conectividad**, no en las otras categorías | Catálogo de soluciones (hover-reveal) | Bug/feature | **Hecho** | Era contenido: solo Conectividad tenía descripciones. Autocompletadas las 3 categorías (27 items) desde el intro del subservicio. Cliente puede refinar en Tina. |
| 15 | Notar **más el degradé de fondo** | Form "Déjanos tus datos" (subservicio) | Mejora diseño | **Hecho (best-effort)** | Degradés de fondo del form subidos. |
| 16 | Otro **degradé a la derecha**, más fuerte, **efecto grano**, 3 tonalidades (no morado→negro) | Soluciones slider · desktop | Mejora diseño | **Hecho (best-effort)** | Ídem 10 (glow ambos lados, grano, multi-tono). |
| 17 | En pantallas **grandes** el formulario se aleja mucho; acercarlo | Contacto · desktop grande | Mejora responsive | **Hecho (best-effort)** | Grid acotada a 1200 + gap menor → form más cerca en pantallas grandes. |
| 18 | **Fade/desvanecimiento** en los bordes al deslizar (sin corte brusco) | Carruseles ISO + Soluciones | Mejora diseño | **Hecho (best-effort)** | Máscara de fade en el borde derecho de carruseles soluciones + ISO. |
| 21 | Aumentar el tamaño del **tag "[ CASOS DE USO ]"** (similar a soluciones/partners) | Subservicio · sección Casos de uso | Mejora | **Hecho** | Eyebrow `text-caption-sm` → `text-xs md:text-sm` (como partners). La statement se mantiene igual. |
| 20 | Header **opaco/blur desde el inicio** (sin scroll) para que el menú no se pierda sobre el hero de imagen | Páginas de solución (`/soluciones/*`) | Mejora | **Hecho** | Prop `solidOnLoad` en el header, activada por ruta en BaseLayout. Home y demás siguen transparentes al tope. |
| 19 | Subir **opacidad del glass** de los forms para que se note más | Forms de soluciones ("¿Conversamos?") | Mejora diseño | **Hecho** | Glass de forms de hero (subservicio+solución) subido a bg-white/[0.08] + border/blur. |
| 22 | Los **años se cortan** (reposo y en el efecto) | Timeline (Nosotros) | Bug visual | **Hecho (verif. Playwright)** | 3 causas: (1) vertical — `leading-none` deja la caja de línea de Poppins (~1.4em) fuera de la ventana `overflow-hidden` → `py-[0.24em]`; (2) horizontal en reposo — `tracking-tighter` cortaba el último dígito; (3) horizontal **en la transición** — Poppins no tiene cifras tabulares, así que "2006" (0/6 anchos) es más ancho que p.ej. "2012"; la ventana se dimensionaba al año entrante y recortaba el saliente. Fix definitivo: ancho fijo `w-[3em]` + `text-center` → todos los años comparten ventana de 765px, ninguno se recorta (margen ≥88px). Verificado ciclando los 9 hitos: 1 solo ancho de ventana, clip 0. |
| 23 | Subir **2 puntos el texto** de las cards, que se note más | Valores (Nosotros) | Mejora | **Hecho** | Texto de card `text-sm` → `text-base md:text-lg` (16→18px). |
| 24 | **Aumentar tamaño** de textos | Formas de pago | Mejora | **Hecho** | Pasos: título 28→32px, instrucciones 18→20px; intro hero 22→26px; selectores banco/método `text-body-md`→`text-lg`. |
| 26 | **Scroll horizontal** al pasar por la zona de los hitos | Nosotros (y todo el sitio) | Bug (layout) | **Hecho (verif. Playwright)** | NO era el timeline (su `overflow-hidden` recorta todo). Causa: los reveals con desplazamiento horizontal (`data-reveal="left/right"` + scrub que se aleja ×1.7 al salir → hasta ~170px) trasladan elementos fuera del viewport → scroll horizontal (p.ej. MissionVision al salir por arriba mientras bajás al timeline). Fix global: `#a11y-content { overflow-x: clip }` en BaseLayout (elemento no-root porque `html` no propaga `clip`; no crea scroll-container → no rompe sticky/Lenis/carruseles ni el header fixed). Verificado: scrollX=0 en todas las posiciones en Nosotros y Home. |
| 25 | El **logo FIBERLUX** queda encima del searchbar | Home hero + buscador | Bug (z-index) | **Hecho (verif. Playwright)** | NO era Spline: es el `<img>` del logo del header (SPEC 39) que con el scroll se agranda y baja al hero. El header es `fixed z-[80]` (stacking context) → el logo quedaba sobre el overlay `z-[55]`. El cliente pidió que se vea **debajo** del buscador (no ocultarlo). Fix: subir el overlay del buscador a `z-[85]` (sobre el header) → su backdrop atenúa/difumina el logo (y todo el header) igual que al hero. Nota: el nav también queda atenuado detrás (look de modal). |

| 27 | Las **partículas** rompen el scroll / saltan al arrastrar para recargar | Hero Nosotros (y donde esté NodeField) · **mobile** | Bug | **Hecho (verif. Playwright)** | Causa: el navegador móvil dispara `resize` al mostrar/ocultar la barra de URL y el efecto **re-sembraba todas las partículas** en cada evento. Fix: re-siembra solo ante cambios de layout reales + `resize` coalescido a un frame; sin interacción de puntero en táctil (`hover: hover` + `pointerType === "mouse"`); ~30fps en dispositivos sin hover. Mismo guard de puntero en `LightHalo`. |
| 28 | Quitar el **glow del logo** de Fiberlux | Home (hero + intro FLX→FIBERLUX) | Ajuste diseño | **Hecho** | Eliminado el `drop-shadow` en `HeroLogoIntro` y en el logo hero del header (prop `logoGlow` borrada de la cadena BaseLayout → HeaderV2 → HeaderV2React). |
| 29 | Quitar "Ver todas" y listar **todos los servicios** de cada categoría (máx. 8; con más de 8, "Ver todas") | Soluciones scroll-jack (home, soporte y /soluciones) | Mejora | **Hecho (verif. Playwright)** | Tope único de 8 en desktop (antes: 6 en home / todos en /soluciones). Conectividad (8), Data Center (6) y Servicios gestionados (7) se listan completos; Ciberseguridad (13) muestra 8 + "Ver todas (13)". **Mobile:** el pin de 100svh solo admite 5 filas (a 667px de alto la columna de texto ya ocupa ~2/3), así que ahí quedan 5 + "Ver todas"; filas compactadas y tipografía móvil levemente reducida para que entre. |
| 30 | Agregar la sección de **rubros** debajo de casos de éxito (dar más largo a la página) | Casos de éxito | Mejora | **Hecho** | Se reutiliza el mismo bloque editable de Nosotros (`RubrosReact` sobre la colección `about`) al final de `/casos-de-exito`. Edita en un solo lugar de Tina y se refleja en ambas páginas. Solo en Casos de éxito lleva un **degradado radial morado tenue** (prop `glow`) para separarla del slider de arriba; en Nosotros sigue plana. |
| 31 | Los nodos **se juntan/apelotonan** cuando el mouse se queda quieto | NodeField (hero Nosotros, Soluciones, Home) | Bug visual | **Hecho (verif. Playwright)** | El cursor **aceleraba** las partículas (fuerza acumulativa) → con el mouse quieto colapsaban en un grumo. Ahora el cursor solo aplica un **desplazamiento acotado** (máx. 14px, con falloff) sobre la posición de deriva: se mueven apenas y nunca se acumulan. Verificado 8s con el puntero fijo: campo uniforme. |

| 32 | Hero: **bajar el título/descripción** y recortar el espacio inferior (mucho vacío) | Nosotros · **mobile** | Ajuste layout | **Hecho (verif. Playwright)** | Solo mobile: alto del hero `88vh → 74vh` y padding del bloque `pt-28/pb-16 → pt-40/pb-10` (el texto baja ~60px y el vacío bajo el subtítulo se reduce). Desktop intacto. |
| 33 | La animación de "Comprometidos con el…" **tarda demasiado**: hay un tramo sin texto en pantalla | Misión/Visión (Nosotros) · **mobile** | Ajuste animación | **Hecho (verif. Playwright)** | El scrub usaba la ventana de desktop (entra 0–25%, sale 65–100%) y en mobile el bloque casi llena la pantalla → se desvanecía estando aún visible. En mobile: `0–12% / 12–88% / 88–100%` y recorrido 100→56px. Medido: opacidad 1 desde que el bloque asoma hasta que casi sale. |
| 34 | Mover **"Potencia tu conectividad empresarial" + RRSS al inicio** del footer, y debajo el resto | Footer · **mobile** | Ajuste layout | **Hecho (verif. Playwright)** | Se quitó la inversión de orden en mobile (`order-2 lg:order-1` → `order-1`): el tagline y las redes abren el footer y los grupos de enlaces van debajo. Desktop igual (tagline a la izquierda). |
| 35 | Las **líneas del hero se cortan** sin completar el ciclo (entrecortado) | Soporte técnico (SupportHub) | Bug animación | **Hecho** | El `stroke-dashoffset` iba de `1` a `-0.28`, pero el patrón (0.28 + 0.72) mide exactamente `pathLength = 1`: el recorrido de 1.28 no empalmaba y el pulso **retrocedía** al reiniciar (y nunca llegaba al nodo). Ahora va `1 → 0` = un período exacto → bucle continuo. |
| 36 | Dejar **solo el isotipo de Fiberlux** en el ícono central (sin los otros íconos) | Soporte técnico (SupportHub) | Diseño | **Hecho** | Eliminado el ciclo de íconos (audífonos/rayo/server) junto con su `setInterval` + IntersectionObserver. El núcleo lleva el **trazado real** del isotipo (`public/images/soporte-tecnico/isotipo-fiberlux.svg`) inline con `fill: currentColor` en el rosa claro del hub + glow suave; ancho 58% y alto automático (es apaisado 54×39, no se deforma). |

| 37 | En **mobile**: título + imagen que entren de **izquierda a derecha** y misión/visión de **derecha a izquierda** | Misión/Visión (Nosotros) · **mobile** | Ajuste animación | **Hecho (verif. Playwright)** | La columna era un único scrub. Ahora se parte en dos bloques (`título + imagen` con `data-reveal="left"`, `misión + visión` con `="right"`), activos **solo en mobile** vía el nuevo atributo `data-reveal-only="mobile\|desktop"` del motor de reveals (`src/scripts/reveal.ts`); el bloque que no aplica se muestra sin animar. En desktop sigue corriendo el scrub único de la columna + la imagen desde la derecha, sin cambios. |
| 38 | El **degradado del planeta** debe parecerse a la referencia del cliente; **bajar el texto** para que no choque con el planeta y **transparentar los puntos** en la zona del titular | Hero Home (`cinematic`) | Diseño | **Hecho (verif. Playwright)** | Halo rehecho: en vez de una banda ancha y lavada, una **línea de luz fina y brillante justo en el limbo** (núcleo casi blanco) con bloom largo hacia afuera y caída rápida hacia adentro (interior oscuro). El planeta se agranda (`w*1.15 → w*1.32`, mismo ápice): el limbo queda **más plano** y el arco cruza por fuera del ancho del titular en vez de atravesarlo. El texto baja ~28px en desktop (`lg:pt-40 → lg:pt-[13rem]` + `min-h 640 → 600`), el resplandor de base se contiene (casi negro alrededor del planeta), `mapBrightness 1.0 → 0.85` y el velo radial de legibilidad se recoloca sobre la banda del texto (`50% 56%`, 0.78). **Sin coste extra por frame**: el halo pasó a sprite precomputado y se descartó una máscara CSS sobre el canvas WebGL que costaba ~2.5 ms/frame (mismo resultado sobre negro con el velo). Medido: 8.3 ms mediana antes y después. |
| 39 | Los **puntos se van para los costados / se quedan pegados al borde** cuando la escena lleva rato | Hero Home (`cinematic`) | Bug | **Hecho (verif. Playwright)** | Dos causas. (a) El canvas 2D se creaba con el ancho del root **antes** de que apareciera el scrollbar y cada frame se limpiaba sólo `root.clientWidth`: la franja sobrante **nunca se borraba** y acumulaba una costra de puntos brillantes pegada al borde derecho, que crecía con el tiempo → ahora se limpia todo el buffer y un `ResizeObserver` mantiene la geometría en sincronía. (b) La atracción del cursor movía la **posición** de las estrellas sin devolverlas: tras un rato el campo quedaba apelmazado → ahora la deriva base es constante con envolvente por los bordes (uniforme para siempre) y el cursor sólo mueve un **offset con muelle** que vuelve a cero. Verificado: 45 s de escena + barrido de cursor → densidad idéntica en ambos bordes, sin acumulación. |
| 40 | El **mundo se ve como un arco de neón**, no como un mundo (referencias Figma/Sellix/Anker) | Hero Home (`cinematic`) · desktop | Diseño | **Hecho (verif. Playwright)** | Tres causas juntas: (a) el limbo era una **línea de luz finísima y casi blanca** (obs_38) → sobre un planeta negro sólo se leía el filo = arco de neón; (b) `mapBrightness 0.85` dejaba los **continentes invisibles** (disco negro, sin "mundo"); (c) el globo medía 1.06× el ancho del viewport → sus costados quedaban **fuera de pantalla** y el limbo cruzaba plano de borde a borde. Fix: halo rehecho como **atmósfera ancha y suave** (banda de luz ~10% del radio en vez de ~3%, bloom hasta 1.6R, interior casi limpio) + **luz direccional** (más intensa arriba, se apaga hacia abajo, como en las referencias); continentes visibles (`mapBrightness 0.85 → 3.2`, `mapBaseBrightness 0.008 → 0.055` para que el océano dé silueta, `mapSamples 14k → 20k`, puntos en blanco lila, `diffuse 2.2 → 1.5`); y **diámetro 0.9× el ancho** (cap `h*1.34`) para que la curvatura de la esfera entre en pantalla. |
| 41 | En **mobile** el mundo debe verse como en desktop (que se vea **medio mundo**) | Hero Home (`cinematic`) · mobile | Coherencia diseño | **Hecho (verif. Playwright)** | En mobile el globo medía 1.32× el ancho del *root* y entraba **completo** en la pantalla: se leía como un anillo cerrado alrededor del texto, no como un planeta. Ahora la geometría se calcula sobre el **diámetro real** de los puntos con una rama propia para viewports < 768px (`max(w*1.45, min(h*0.88, w*2.4))`, ápice a `0.13·h`): el planeta llega hasta el borde inferior del hero y se **recorta**, igual que en desktop. El limbo se refuerza un 45% en mobile (va bajo el velo oscuro de legibilidad). **Ajuste posterior del cliente** ("el fondo choca con las letras"): se subió la opacidad de los velos radiales de legibilidad en ambos breakpoints — mobile 0.62/0.5/0.32 → **0.72/0.6/0.3** y desktop 0.78/0.6/0.24 → **0.9/0.78/0.42**, con la elipse un poco más ancha (52%×42% → 58%×48%) para cubrir también la banda de los botones. **Rendimiento:** el canvas pasó a cubrir toda la pantalla, así que se bajó el cap de DPR móvil 1.5 → 1.15 (los puntos no necesitan retina); medido 8.3 ms/frame, igual que antes. |
| 42 | El **hero de Soluciones** necesita más impacto: mantenerlo pero pulirlo / combinarlo con algo | Soluciones (`HeroServicios`) | Diseño | **Hecho (verif. Playwright)** | Se probaron dos caminos. **(A) índice de categorías dentro del hero** (tiles glass enlazando a cada solución, con onda de luz al hover) — **descartada por el cliente**. **(B) titular cinético — implementada:** el H1 pasa a tres líneas, `Soluciones para` · **verbo que rota** (conectar → proteger → escalar → operar, degradado blanco→magenta) · `tu empresa`, con barrido enmascarado (sube, se desenfoca y entra el siguiente) cada 2.6 s; cada palabra que aterriza **dispara la onda de luz** del shader detrás del titular (API imperativa nueva `pulse(u,v)` en `CinematicRays`), así fondo y texto laten juntos. Todo editable en Tina (`headingPrefix`/`headingWords[]`/`headingSuffix` con sus `_en`); lista vacía = titular fijo de siempre. Pulido que se conserva de la ronda A: titular 52 → **68px** en desktop, **antetítulo** editable (`eyebrow`), empalme en degradado con el bloque siguiente, breadcrumb "Inicio" localizado. Dos bugs de paso: el CTA apuntaba a `#soluciones-scroll` (sección que ya no existe; ahora `#soluciones-stack`) y las categorías del CMS llegaban al componente y **se descartaban** (ya no se consultan). |

---

## Agrupaciones sugeridas (para specs)

- **A · Motor de sliders (flechas + drag):** 2, 7, 8 → un solo fix del `useDragSlider` (flechas que no avanzan / drag a medias).
- **B · Glass / degradados / grano:** 5, 10, 15, 16, 19 (y en parte 9) → tratamiento visual unificado de cards y forms (más glass, degradé más fuerte, grano, visibilidad en bajo brillo).
- **C · Testimonios ↔ Casos de éxito (contenido + logo/comillas):** 11, 12, 13 → alinear contenido y agregar opción logo.
- **D · Bugs visuales sueltos:** 1/9 (raya blanca), 3 (lag testimonios), 14b (catálogo solo en Conectividad).
- **E · Ajustes puntuales:** 14a (vector por categoría), 17 (spacing contacto), 18 (fade edges), 4 (diferido), 6 (aclarar).

---

## Bitácora

- 2026-07-21 — Registradas 19 observaciones (20 ítems) desde `~/Downloads/observaciones_fiberlux/`. Todas en `Pendiente` salvo #4 (Diferido) y #6 (Necesita aclaración).
- 2026-07-21 — Cliente aclara con referencias: **#6** (ref. íconos de rubros → Pendiente), **#4** (ya no diferido: degrade de profundidad + color en números → Pendiente), **#10/#16** (glass soluciones: ref. Figma con grano + multi-tono + glows ambos lados). Orden confirmado: **A (bugs slider) → D (bugs visuales) → B (glass: soluciones → ¿por qué Fiberlux? → ISO) → C (contenido)**.
- 2026-07-21 — Lote de fixes en rama `fixes-observaciones-batch`: A (2,7,8), D (1,3,9,14b), C (11,12,13), + 19. Build OK. **Pendiente (best-effort de diseño):** 4, 5, 6, 10, 15, 16, 18. Diferido: 14a.
- 2026-07-21 — Cluster B + puntuales best-effort hechos (4,5,10,15,16,18) + 6,17. Build OK. **Solo queda 14a (diferido).** Lote listo para 1 deploy. Nota: los ajustes visuales de glass/degradados son best-effort; afinar en el device.
- 2026-07-21 — obs_6: remapeo completo de íconos de rubros a Lucide (outline) por pedido del cliente. Build OK, verificado visualmente.
- 2026-07-21 — Nueva obs_20 (pedido del cliente): header opaco/blur desde el inicio solo en rutas `/soluciones/*` (categorías + subservicios), para legibilidad del menú sobre el hero de imagen. Build OK.
- 2026-07-27 — Nuevo lote (obs 22–25): 22 años cortados en Timeline (padding em al año), 23 texto de Valores +2pt, 24 tamaños de Formas de pago (pasos + intro + selectores, confirmado por el cliente los 3), 25 logo 3D del hero encima del buscador (evento `fbx:overlay` → oculta capa Spline). Build OK (116 páginas).
- 2026-07-28 — Refinamiento tras feedback del cliente: obs_22 seguía cortando el último dígito **a la derecha** (era `tracking-tighter` + overflow-hidden) → agregado `px-[0.16em]` al año. obs_25 el cliente aclaró "es el logo de FIBERLUX, no el 3D" — es el mismo canvas Spline; el fix ya lo cubre (aún no estaba desplegado cuando lo vio). Ambos **verificados con Playwright** en dev local (año completo overflow 0; capa 3D opacity 0 al abrir el buscador real).
- 2026-07-28 (4) — obs_26 (nueva): scroll horizontal "al entrar a los hitos". Diagnóstico Playwright: el timeline no desborda; el causante era MissionVision (`data-reveal="right"` scrub que traslada la imagen +170px fuera del viewport al salir por arriba). Fix global `#a11y-content { overflow-x: clip }` (no en `html` porque el root no propaga `clip`). Verificado scrollX=0 en Nosotros + Home, header fixed OK.
- 2026-07-28 (3) — obs_22 último corte: el cliente vio "2006" cortado a la derecha **durante la transición**. Causa: años de distinto ancho (Poppins sin cifras tabulares) → la ventana del slide se dimensionaba al año entrante y recortaba el saliente más ancho. Fix: `w-[3em] text-center` al año → ventana constante (765px) para todos. Verificado ciclando los 9 hitos (1 ancho, clip 0).
- 2026-08-10 — **SPEC 99 implementado** en rama `spec-99-lote-qa-cursor-soluciones-conectividad` (11 commits, uno por step). Build local verde (116 páginas, 0 errores). obs5 se corrigió tras aclaración del cliente: "sombrear el texto" = **selección de texto** (`::selection` morado), no hover. **Pendiente:** pushear para que TinaCloud reindexe el tipo nuevo `GlobalCursor` (cambio de schema); QA visual en device (ajustes best-effort: glow del cursor, %/halo del globo, fit del pin en mobile). Falta la "imagen de la derecha" de obs10 para calzar el look fino.
- 2026-08-09 — **Nuevo lote (SPEC 99)** — 10 observaciones nuevas del cliente (spec `99-lote-qa-cursor-soluciones-conectividad.md`, estado Borrador): (1) cursor configurable en Tina + menos glow, quitar cursores por sección [default estela]; (2) "Grupo Fiberlux" con G mayúscula en Certificaciones; (3) scroll-jack de Soluciones en mobile con feedback de avance; (4) impulso/snap más ágil en Soluciones; (5) uniformizar la estela del mouse header/footer + hover morado en links; (6) íconos blancos de "El desafío" → línea gráfica + Conectividad "Sedes conectadas" (sin el "3"); (7) íconos flotantes del hero: oscuros al inicio, color al alejarse; (8) footer arranca en negro (transición desde sección negra); (9) quitar sombreado de partners; (10) globo de conectividad del hero home (`CinematicBackground`): más líneas, halo más grande, planeta más oscuro, 50% visible (no 60%). Ambigüedades cerradas con el cliente vía /spec. Pendiente de aprobar e implementar.

- 2026-07-28 (2) — Correcciones tras 2ª ronda de feedback: **obs_25** el cliente mostró DevTools: NO es Spline, es el `<img>` del logo del header. Revertido el fix del 3D (HeroHomeReact + evento en SearchOverlay). Pidió que el logo se vea **debajo** del buscador (no ocultarlo) → subido el overlay a `z-[85]` sobre el header. **obs_22** el cliente seguía viendo corte; medición de tinta (Range) mostró que la ventana recortaba ~20px de la caja de línea arriba/abajo → subido `py` de `0.12em` a `0.24em` (ventana 377px contiene tinta 358px). Verificado con Playwright (clip negativo en 4 lados; capturas en reposo, ideal y mid-animation).
- 2026-08-10 (2) — **Lote nuevo (obs 27–31)** del cliente (Jean Pierre, WhatsApp): 27 partículas rompiendo el scroll en mobile, 28 quitar glow del logo, 29 listar hasta 8 subservicios en el scroll-jack de Soluciones, 30 rubros al final de Casos de éxito, 31 nodos que se apelotonan con el mouse quieto. Todo implementado sobre `main`; build local verde (116 páginas) y verificación visual con Playwright (desktop 1440×900 y mobile 390×667/780). **Nota para el cliente:** en mobile el scroll-jack muestra 5 subservicios + "Ver todas" (8 no entran en pantalla sin cortar el bloque).
- 2026-08-10 (4) — **Lote (obs 32–36)**: hero Nosotros mobile más compacto, scrub de Misión/Visión más ágil en mobile, footer mobile con tagline+RRSS arriba, pulso del hero de Soporte en bucle continuo y núcleo con el isotipo de Fiberlux (fin del ciclo de íconos). Build local verde (116 páginas); verificado con Playwright (390×780 y 1440×900).
- 2026-08-10 (5) — obs_37: nuevo atributo genérico `data-reveal-only` en el motor de reveals para limitar una animación a mobile o desktop (el breakpoint que no aplica se resetea a visible, porque el CSS anti-FOUC deja los `[data-reveal]` en opacity 0). Usado para partir Misión/Visión en dos direcciones solo en mobile. Build local verde (116 páginas).

- 2026-08-27 — **Lote (obs 38–39)** — hero Home (globo `cinematic`): degradado del limbo al estilo de la referencia del cliente (línea de luz fina + bloom), planeta más grande/plano para que el arco no cruce el titular, texto ~28px más abajo en desktop, puntos atenuados tras el texto; y **bug de los puntos pegados al borde** (franja del canvas que nunca se limpiaba + arrastre permanente por el cursor). Verificado con Playwright a 1440×900 y 390×844, con medición de frames (8.3 ms mediana, igual que antes del cambio) y de densidad de puntos en los bordes tras 45 s.

- 2026-08-27 (2) — **Lote (obs 40–41)** — hero Home (globo `cinematic`): el cliente pidió que el planeta se lea como un **mundo** y no como un arco de neón (referencias de Figma: Sellix, Anker, Quantum), y que en **mobile** se vea *medio mundo* como en desktop. Se revierte parcialmente el criterio de obs_38 (limbo de filo fino): ahora el halo es una **atmósfera ancha y direccional**, los **continentes son visibles** y el globo se dimensiona por su diámetro real (0.9× el ancho en desktop; en mobile se agranda y se recorta contra el borde inferior del hero). Verificado con Playwright a 1920×1080, 1440×900, 1280×680, 834×1112, 390×844 y 360×640, en reposo y con scroll. Rendimiento sin cambios (8.3 ms/frame; en desktop el canvas es ~50% más chico que antes y en mobile se compensó bajando el cap de DPR a 1.15). `astro build` verde.

- 2026-08-28 — **obs 42** — hero de Soluciones: se conserva el fondo cinematic (god-rays + tiles flotantes) y se le añade el **índice de categorías** dentro del hero, con hover que emite la onda de luz del propio shader; titular más grande, antetítulo editable, empalme con la sección siguiente. Arreglados el CTA que apuntaba a una sección inexistente y el prop `categories` que se descartaba. Verificado con Playwright a 1440×900, 390×844 y 360×640, en ES y EN (URLs `/en/...`, etiquetas y antetítulo traducidos). `astro build` verde (108 páginas).

- 2026-08-28 (2) — **obs 42 · vuelta B** — el cliente descarta el índice de categorías en el hero (opción A) y elige el **titular cinético**. Se revierte la fila de tiles y la query de `home` en `HeroServicios.astro` (el hero vuelve a depender solo de la colección `servicios`); se conserva y reutiliza la API `pulse(u,v)` de `CinematicRays`, ahora disparada por el cambio de palabra en vez del hover. Ojo con el enmascarado: la ventana del verbo necesita **0.4em de aire abajo** (compensado con margen negativo) porque a 68px la cola de la *g* de "proteger" se salía 5px y se recortaba —misma clase de bug que obs_22 con los años del timeline—; medido con `measureText` en los 4 verbos, márgenes positivos arriba y abajo. Verificado con Playwright a 1440×900 y 390×844, ES y EN, con el ciclo completo de palabras y el pulso de luz confirmado en consola (cada 2.6 s en 0.500/0.442, el centro del verbo). `astro build` verde (108 páginas).

- 2026-08-28 (3) — **obs 42 · ajuste tras feedback** ("la transición está fuerte y *proteger* se ve cortada"): las dos cosas venían del mismo sitio. El barrido usaba una **ventana `overflow: hidden`** y, aunque en reposo la cola de la *g* entraba justa (4–5px de tinta libre), **mientras la palabra subía se recortaba** contra el borde. Se quita la máscara entera: ahora es un **cruce sin recorte** (nada que cortar), con recorrido más corto (0.62em → **0.18em**), **sin desenfoque**, y **escalonado** — la que entra arranca 170ms tarde, cuando la que sale ya está casi apagada; sin ese retardo las dos palabras se leían encimadas a media opacidad. Además el ciclo pasa de 2.6 s a **3.2 s**. Medido con muestreo por frame: **0 frames** con las dos palabras por encima de 0.5 de opacidad (se cruzan en ~0.37/0.40). `astro build` verde (108 páginas).

- 2026-08-28 (4) — **obs 42 · causa real del corte de "proteger"**: no era la máscara (por eso seguía cortada tras quitarla). El verbo va con **`background-clip: text`** y la caja que se pinta es la del *padding* del span: la cola de la **g** cae por debajo de esa caja, se queda **sin pintar** y se ve transparente. Es la única palabra de la lista con descendente — justo la que reportó el cliente. Fix: `padding-bottom: 0.3em` + `margin-bottom: -0.3em` en el span del verbo (la caja pintada baja, el alto del titular no cambia) y paradas del degradado recolocadas (55%/100% → 43%/78%) para que el tramo sobre las letras se vea igual. Verificado con zoom sobre "proteger" congelado: descendentes de la *p* y la *g* completas y con degradado. Además, **el cliente pide quitar el antetítulo** "5 CATEGORÍAS · RED 100% FIBRA ÓPTICA": se vacía el contenido (`eyebrow`/`eyebrow_en` fuera del JSON); el campo sigue en Tina por si lo quieren recuperar, y si está vacío el hero no lo renderiza. `astro build` verde (108 páginas).

- 2026-08-28 (5) — **obs 43** (pedido del cliente): el hero de la categoría **Infraestructura** usaba prestada la foto de Comunicaciones (`categoriaserviciosgestionados1.webp`). Se apunta a la imagen nueva que subió el cliente, `images/soluciones/servicio-infraestructura.webp` (2756×1536, torre de telecomunicaciones sobre ciudad de noche) en `services/infraestructura.json` → `hero.heroImage`. El `heroMode` sigue en `image`, así que entra por el mismo camino que las demás categorías: foto a sangre a la derecha en desktop (`right center`) y arriba en mobile (`center top`), con la malla `NetworkDepth` y el velo de contraste encima. El sujeto —la torre— queda a la derecha, justo donde el velo no tapa. Verificado con Playwright a 1440×900 y 390×844. `astro build` verde (108 páginas).

- 2026-08-28 (6) — **Lote (obs 44–48)** — cinco observaciones sobre carruseles y paneles:
  **44** Certificaciones, fondo estático → las cintas de luz ahora **derivan** (transform/opacity, períodos 11/13/15/17 s, sin sincronizar) con el mismo lenguaje del hero de Soluciones; la rotación base viaja en `--rot` para que los keyframes sirvan en mobile y desktop, y `prefers-reduced-motion` las detiene. **Hicieron falta dos pasadas más porque el problema no era la amplitud sino el EJE.** Los porcentajes de `translate` se resuelven contra la caja del propio elemento, y estas cintas miden ~2100px de ancho por ~280px de alto: mover un 6% en X son 126px sobre una elipse de luz que ya ocupa los 2100px → la imagen no cambia; ese mismo 6% en Y son 17px. Los dos primeros intentos movían sobre todo en X, por eso el cliente seguía viendo el fondo congelado aunque las animaciones sí corrían. Solución: vaivén por el **eje corto** (±22% del alto en las cintas ≈ 62px, ±45% en el filamento ≈ 36px = la mitad de su grosor) + **bamboleo de inclinación** de ±2.4° (con 2100px de brazo, dos grados desplazan los extremos ~45px) + vaivén de opacidad 0.55→1. Verificado siguiendo la **fila del píxel más brillante** por columna en dos fotogramas a 3 s: el haz sube 46px en la columna 600 y su brillo pico cae un 21%.
  **45** Casos de éxito, card pegada al borde → `align: "center"` + `loop`, con la separación movida del `gap` del contenedor al `px` de cada slide (así Embla mide el slide **con** su canal y el bucle puede reubicar el vecino del otro extremo). El destrabe real fue en `useSlider`: el tween `scale` escribía `transform` **en el nodo del slide**, la misma propiedad que usa el looper de Embla, y le borraba la reubicación → ahora escala la capa interior. Los tres casos quedan con vecino a ambos lados y la card centrada (peek de 126px por lado a 1512px).
  **46** Soporte técnico, acordeón de escritorio con `height: 560` fija que recortaba en silencio al agregar filas desde el CMS → el alto lo dicta ahora el panel abierto (medido con ResizeObserver, animado, con 560px como **mínimo** para conservar la proporción del diseño). Verificado con 6 filas extra: crece a 946px sin recorte.
  **47** Titular de Soluciones, "salto de margen" al cambiar de palabra → eran dos cosas: la caja del verbo saltaba de un ancho a otro (ahora los anchos se **miden** sobre una copia oculta tras `fonts.ready` y el ancho se anima) y la palabra saliente, anclada con `left:0` sobre la caja nueva, se desplazaba lateralmente (ahora va centrada). Además el cruce se solapa: antes había ~150 ms sin ninguna palabra visible.
  **48** Certificaciones, borde cortado en seco por el `overflow-hidden` al cambiar de slide → **máscara horizontal** en el viewport, con el ancho del degradado igual al `px` del slide, de modo que la card en reposo queda íntegra y sólo se desvanece lo que entra o sale del encuadre.
  **49** (mismo pedido, en Casos de éxito) los casos vecinos asomaban **cortados en seco** a cada lado → misma máscara, pero aquí el vecino es visible SIEMPRE (no sólo en la transición), así que el degradado tiene que cubrir todo su trozo visible sin tocar la card activa. Como esa distancia cambia con el ancho de ventana, se calcula en CSS: `max(28px, calc((100% - 936px) / 2 + 28px))` — 936 = 880 de card + 56 de canal. A 1512px da 244px de desvanecido y la card activa arranca exactamente en 244 (margen 0, nitidez intacta); a 1024px da 28px con 21px de holgura; en móvil cae al `px` del slide (12px). Verificado a 1512/1024/390 sin scroll horizontal.
  Verificado con Playwright a 1512×900 y 390×844 (sin scroll horizontal en móvil: el desborde del viewport se limita al gutter del `site-container`). `astro build` verde (108 páginas).

- 2026-08-28 (7) — **Lote (obs 50–52)**:
  **50** Casos de éxito, el play sólo respondía en el círculo de 56px → el disparador pasa a cubrir **toda la foto** (sigue siendo un `<button>` real, con foco por teclado; el círculo queda como decoración `aria-hidden` dentro). Se añade un velo que oscurece la imagen al pasar el cursor, porque con el botón invisible a pantalla completa no había ninguna señal de que fuera clicable, y la chapa de "CASOS DE ÉXITO CON FIBERLUX" pasa a `pointer-events-none` para no abrir un agujero en el área de clic. El arrastre del carrusel no dispara el video: Embla cancela el `click` que sigue a un gesto. Verificado con `elementFromPoint` en las 4 esquinas y sobre la chapa (todas dan en el botón) y abriendo el modal desde una esquina.
  **51** Tooltip "Ver video" al pasar el cursor sobre la foto, extraído a **`shared/CursorTooltip.tsx`**. Ojo con cuál de los dos tooltips del repo es: primero se usó el **oscuro** de `SolucionesStackReact` (el que sigue vivo en Home) y el cliente aclaró con una referencia que quería el **claro** — píldora blanca translúcida con el glifo ↵, centrada sobre el cursor, que es la de `SolucionesPanelReact` (código muerto). Es la que está puesta. Dos detalles obligados: va por **portal a `document.body`** (es `position: fixed`, y el `transform` del contenedor de Embla se convertiría en su bloque contenedor y lo dejaría desplazado y recortado dentro del carrusel) y la posición se escribe por `ref` dentro de un `requestAnimationFrame`, no por estado, para no repintar la card en cada `mousemove`. Sólo en punteros finos y `lg+`; respeta `prefers-reduced-motion`. Texto en `i18n/ui.ts` (`casos.vervideo`: Ver video / Watch video). **Nota:** los componentes que traían el tooltip original —`SolucionesPanel.astro` y `SolucionesScroll.astro`— siguen en el repo pero **ya no se usan en ninguna página**; se dejan intactos, pendientes de decidir si se borran.
  **52** Footer, logo de TWNSTUDIOS descolgado respecto a la línea de copyright → no bastaba `align-middle`: la envoltura era un `inline-block` con la imagen `inline` dentro, así que su caja no medía los 16px del logo sino la línea entera del texto (con el hueco de los descendentes por debajo), y centrar ESA caja dejaba el logo 2.5px bajo el centro óptico de la frase. Ahora la envoltura mide exactamente una línea (`1lh`, con `1.5em` de reserva) y se ancla arriba, con el logo centrado dentro por flex. Error medido: **2.5px → 0.1px**.
  `astro build` verde (108 páginas), sin errores de tipos nuevos.

- 2026-08-28 (8) — **obs 53** — al refrescar, Casos de éxito se veía un instante con las cards **pegadas al borde izquierdo** y luego saltaban al centro. Es el hueco entre el HTML estático y la hidratación: hasta que la isla no monta, Embla no ha escrito el `transform` que centra la card, así que se pinta el flujo crudo (slides en fila desde el margen). Fix en dos partes: (a) el viewport nace en `opacity: 0` y sólo se muestra —con un fundido de 260ms— cuando Embla ya midió (`scrollSnaps.length > 0` → `data-listo="1"`), con un keyframe de red de seguridad a los 2s por si el JS nunca llega a ejecutarse, para no dejar el bloque en negro; (b) `client:visible` → **`client:load`** en `CasosSlider.astro`, porque el carrusel es lo primero que se ve y esperar al observador de visibilidad sólo alargaba el hueco. Verificado sobre el HTML servido (el viewport llega sin `data-listo`, con la regla de opacidad 0 y el keyframe presentes) y midiendo el fundido (0 → 0.34 → 1).

- 2026-08-29 — **obs 54** — el stack de Soluciones en **móvil**: el cliente final "casi no notó el borde" de las cards en su celular. Ya existía un realce por proximidad (`--sol-cerca`, 0→1 según lo centrada que esté la card), pero era un aro de **1px al 42%** de magenta sobre negro: en una pantalla chica, con brillo automático, eso es invisible. Se hacen las dos cosas que pidió el cliente: (a) el aro pasa a **2px y 80%** y se le suma por fuera un **halo desenfocado de 22px al 30%** — en pantalla pequeña el halo es lo que de verdad delata el borde, porque ocupa varios píxeles en vez de uno; el borde base sube de 14% a 18%; (b) la card **crece un 1.2%** al centrarse. El crecimiento va en la propiedad `scale` y **no** en `transform`, porque el motor de reveals escribe `transform` en línea sobre ese mismo nodo (lleva `data-reveal`) y se pisarían; `scale` se compone aparte y no tiene coste de layout (no empuja a las vecinas, y 1.2% queda muy por dentro del hueco de 32px entre cards). Con `prefers-reduced-motion` el tamaño no cambia, pero el aro y el halo se quedan — que es lo que hace falta para verla. Sólo aplica bajo `lg`: en desktop el rail lateral ya dice dónde estás. Verificado a 390×844 (sólo se enciende la card centrada, el resto en 0; sin scroll horizontal). `astro build` verde (108 páginas).
